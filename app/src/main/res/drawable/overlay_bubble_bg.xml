<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Diâmetro fixo da bolha -->
    <size
        android:width="28dp"
        android:height="28dp" />

    <!-- Fundo translúcido -->
    <solid android:color="#F0FFFFFF" />

    <!-- Borda suave -->
    <stroke
        android:width="1dp"
        android:color="#22000000" />

    <!-- Respiro interno leve -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" />
</shape>
